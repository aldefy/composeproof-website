---
const steps = [
  {
    number: '01',
    title: 'Run the setup wizard',
    description: 'One command. Installs the binary, picks your AI agents (Claude Code, Gemini CLI, Cursor), configures MCP, and optionally installs the Compose UI skill.',
    code: 'npx composeproof',
  },
  {
    number: '02',
    title: 'AI learns your patterns',
    description: 'Optionally scan your Claude Code sessions for Compose corrections. Your past fixes become anti-patterns that every AI agent avoids.',
    code: 'npx composeproof learn',
  },
  {
    number: '03',
    title: 'AI has eyes',
    description: 'Your AI assistant renders previews, inspects live devices, verifies screenshots, and iterates on UI autonomously — with the skill guiding every step.',
    code: 'You: "verify the login screen"\nAI:  renders → reviews → fixes → PASS ✓',
  },
];
---

<section class="py-20 lg:py-28">
  <div class="max-w-7xl 2xl:max-w-[85%] mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-3xl sm:text-4xl font-bold mb-4">
        Up and running in <span class="gradient-text">3 steps</span>
      </h2>
      <p class="text-lg text-slate-400 max-w-2xl mx-auto">
        Zero-install architecture. No changes to your project's build files.
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-10">
      {steps.map((step) => (
        <div class="relative">
          <div class="text-7xl font-bold text-slate-500 mb-4 font-mono" aria-hidden="true">{step.number}</div>
          <h3 class="text-2xl font-semibold mb-3">{step.title}</h3>
          <p class="text-slate-400 mb-5">{step.description}</p>
          <div class="rounded-lg border border-brand-border bg-brand-darker p-5 font-mono text-sm">
            <pre class="text-slate-300 whitespace-pre-wrap">{step.code}</pre>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
